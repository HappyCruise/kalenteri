/*
import React, { Component } from "react";


class Test extends Component {
    
    render(){
        return(
            <div>
                <p>tEsTi CoMpOnEnTtI</p>
                <ul>
                    {this.props.events.map(el=> (
                        <h1>{el.name}</h1>
                    ))}
                </ul>
            </div>
        )
    }
}

function mapStateToProps(state){
    //events: getSortedEventsList(state)

} 


export default connect(
    mapStateToProps
)(Test);
*/